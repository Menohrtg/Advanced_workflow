name: First Workflow
run-name: First Workflow
on:
    push:
        branches: 
            - main
            - dev
    pull_request: 
        branches: 
            - main
            - dev 
    workflow_dispatch: 
jobs:
    build:
        runs-on: windows-latest
        steps:
            - name: 🍏checkout step
              uses: actions/checkout@v4
            - name: 🍏Run an inline script
              run: |
                Write-Host "This is the first github action"
            - name: 🍏Run an inline multi-line
              run: |
                Write-Host "Keep testing the workflow until you get the right one"
                Write-Host " The workflow now works perfectly because you kept trying "
    Test:
        runs-on: windows-latest
        steps:
            - name: checkout step
              uses: actions/checkout@v4
            - name: run a hello script
              run: |
                scripts\hello.ps1
                